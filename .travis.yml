# Travis CI Configuration File

# Tell Travis CI we're using PHP
language: php

# Build matrix options
matrix:
  include:
    - php: 5.6
    - php: hhvm
  allow_failures:
    - php: 5.6
  fast_finish: true

script:
